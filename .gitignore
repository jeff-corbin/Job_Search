# =============================================================================
# SECRETS — never commit these
# =============================================================================
.env
*.env
.env.*

# =============================================================================
# RUNTIME STATE — generated by the app, no need to track in git
# =============================================================================
seen_jobs.json

# =============================================================================
# PYTHON ARTIFACTS — generated automatically, not source code
# =============================================================================

# Bytecode cache — Python compiles .py files to .pyc for faster loading.
# These are machine-generated and specific to your Python version.
__pycache__/
*.py[cod]

# Distribution / packaging (not relevant now, but good habit)
*.egg-info/
dist/
build/

# =============================================================================
# VIRTUAL ENVIRONMENT — if you ever use one (see note below)
# =============================================================================
venv/
.venv/
env/

# =============================================================================
# VS CODE — workspace settings are personal, not project config
# =============================================================================
.vscode/

# =============================================================================
# LOGS
# =============================================================================
*.log

# =============================================================================
# OS JUNK
# =============================================================================
.DS_Store        # macOS folder metadata
Thumbs.db        # Windows thumbnail cache